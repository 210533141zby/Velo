# 1. 激活虚拟环境
conda activate Wiki

# 2. 进入后端目录
cd backend

# 3. 启动 FastAPI 服务
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
或   uvicorn app.main:app --reload


# 1. 进入前端目录
cd frontend

# 2. 启动开发服务器
npm run dev