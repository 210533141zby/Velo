# 启动说明

## 1. 当前状态
- 后端现在可以不带数据库环境变量直接启动。
- 默认数据库为本地 SQLite：`/root/Velo/backend/data/wiki.db`
- 本机当前常用运行方式是：聊天与 RAG 走本地 Ollama，文本补全走 DeepSeek API。
- 当前 RAG 主链路为论文方案：`paper_final`
- 当前常用模型组合：
  - 聊天模型：`qwen2.5:7b-instruct`
  - embedding 模型：`nomic-embed-text:latest`
  - 补全模型：`deepseek-v4-flash`
  - rerank：`BAAI/bge-reranker-v2-m3`
- 后端启动时会自动检查当前 RAG collection；如果 collection 为空，会把数据库里的现有文档自动重建索引。
- Redis 现在已经装好，标准启动建议把 Redis 一起带上，不再默认忽略。

## 2. 重启服务器后的标准启动顺序

### 2.0 先放好 API Key
不要把密钥写死进代码，也不要放进前端。

正确位置：
```bash
/root/Velo/backend/.env
```

可以直接从示例文件复制：
```bash
cd /root/Velo/backend
cp .env.example .env
chmod 600 .env
```

如果 `.env` 丢了，按当前项目的常用配置，最小可用内容如下：
```env
LLM_PROVIDER=ollama
LLM_BASE_URL=http://127.0.0.1:11434/v1
CHAT_MODEL=qwen2.5:7b-instruct
CHAT_TEMPERATURE=0.0

RAG_PIPELINE_MODE=paper_final
RAG_PAPER_FINAL_SOURCE_COUNT=4
RAG_PAPER_FINAL_COMPLEX_SOURCE_COUNT=6
RERANK_DEVICE=auto
RERANK_MODEL=auto

EMBEDDING_PROVIDER=ollama
EMBEDDING_BASE_URL=http://127.0.0.1:11434/v1
EMBEDDING_MODEL=nomic-embed-text:latest

COMPLETION_PROVIDER=deepseek
COMPLETION_MODEL=deepseek-v4-flash

DEEPSEEK_API_KEY=你的真实密钥
DEEPSEEK_API_BASE=https://api.deepseek.com/v1
```

说明：
- `.env` 已被 git 忽略，不会被正常提交。
- 密钥只允许出现在后端 `.env` 或系统环境变量里。
- 前端任何 `VITE_*` 变量都不能放真实密钥，因为会直接下发到浏览器。

### 2.1 启动 Redis
如果系统刚重启，先确认 Redis 在不在：
```bash
redis-cli -h 127.0.0.1 -p 6379 ping
```

如果返回 `PONG`，说明 Redis 已经在跑，可以跳过。

如果没启动，执行：
```bash
redis-server /etc/redis/redis.conf
```

说明：
- 后端虽然支持 Redis 不可用时降级到内存缓存，但标准启动建议把 Redis 一起带上。
- 后端启动日志里如果看到 `✅ Redis 缓存: 连接成功`，说明这一步已经生效。

### 2.2 启动 Ollama
```bash
cd /root/Velo
ollama serve
```

说明：
- 如果 Ollama 已经作为系统服务启动，这一步可以跳过。
- 需要确保 `ollama list` 里至少有这三个本地模型：
  - `qwen2.5-coder:14b`
  - `qwen2.5:7b-instruct`
  - `nomic-embed-text:latest`
- 其中：
  - `qwen2.5:7b-instruct` 用于聊天与 RAG 生成
  - `nomic-embed-text:latest` 用于向量 embedding
  - `qwen2.5-coder:14b` 主要用于本地补全兜底；当前常用补全主通道是 DeepSeek，但建议本地也保留

### 2.3 启动后端
现在默认配置已经写进代码，直接执行即可：

```bash
cd /root/Velo/backend
/root/Velo/.venv/bin/python -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

说明：
- 不再需要手动写 `POSTGRES_SERVER=sqlite`
- 不再需要手动写 `EMBEDDING_PROVIDER=huggingface`
- 当前标准端口仍是 `8000`
- 如果日志里看到 `聊天模型预热完成`、`Rerank 模型预热完成`、`Uvicorn running on http://127.0.0.1:8000`，说明后端已正常就绪
- 如果当前 RAG collection 是空的，后端启动时会自动把数据库中的文档重建索引

### 2.4 启动前端
```bash
cd /root/Velo/frontend
npm run dev -- --host 127.0.0.1 --port 5173
```

### 2.5 打开页面
```text
http://127.0.0.1:5173
```

后端健康检查：
```text
http://127.0.0.1:8000/health
```

Redis 自检：
```bash
redis-cli -h 127.0.0.1 -p 6379 ping
```

## 3. 当前项目常用配置
以本机当前 `backend/.env` 为准，常用运行配置是：

- 数据库：SQLite
- 数据库文件：`/root/Velo/backend/data/wiki.db`
- Redis：本地 Redis，地址 `127.0.0.1:6379`
- 聊天模型：`qwen2.5:7b-instruct`
- 补全主模型：`deepseek-v4-flash`
- RAG embedding：`nomic-embed-text:latest`
- Ollama 地址：`http://127.0.0.1:11434/v1`
- RAG 主链路：`paper_final`

## 4. 代码层默认行为
如果你完全不看 `.env`，只看代码默认值，那么后端基础默认行为是：

- 数据库：SQLite
- 数据库文件：`/root/Velo/backend/data/wiki.db`
- Redis：优先连接本地 Redis，失败就降级内存缓存
- 聊天模型：`qwen2.5:7b-instruct`
- 补全模型：`qwen2.5-coder:14b`
- RAG embedding：`nomic-embed-text:latest`
- Ollama 地址：`http://127.0.0.1:11434/v1`

## 5. 启动后怎么自检
建议按这个顺序检查：

1. `redis-cli -h 127.0.0.1 -p 6379 ping`
2. `ollama list`
3. 打开 `http://127.0.0.1:8000/health`，确认返回 `{"status":"ok"}`
4. 打开 `http://127.0.0.1:5173`
5. 创建或打开一篇文档
6. 在聊天里测试 RAG
7. 如果要确认 Redis 真被后端用了，可以再执行：
```bash
redis-cli -h 127.0.0.1 -p 6379 --scan --pattern 'rag:response:*'
```

## 6. 如果 8000 端口被占用
先检查：

```bash
ss -ltnp | grep ':8000 '
```

如果有旧进程占着，停掉它：

```bash
kill <PID>
```

或者临时改端口：

```bash
cd /root/Velo/backend
/root/Velo/.venv/bin/python -m uvicorn app.main:app --host 127.0.0.1 --port 8002
```

前端一起改代理：

```bash
cd /root/Velo/frontend
BACKEND_URL=http://127.0.0.1:8002 npm run dev -- --host 127.0.0.1 --port 5173
```

## 7. 如果你想后台运行
### 7.1 后台启动 Redis
```bash
redis-server /etc/redis/redis.conf
```

### 7.2 后台启动 Ollama
```bash
nohup ollama serve >/root/Velo/logs/ollama.log 2>&1 &
```

### 7.3 后台启动后端
```bash
cd /root/Velo/backend
nohup /root/Velo/.venv/bin/python -m uvicorn app.main:app --host 127.0.0.1 --port 8000 >/root/Velo/logs/backend.log 2>&1 &
```

### 7.4 后台启动前端
```bash
cd /root/Velo/frontend
nohup npm run dev -- --host 127.0.0.1 --port 5173 >/root/Velo/logs/frontend.log 2>&1 &
```

### 7.5 查看日志
```bash
tail -f /root/Velo/logs/backend.log
tail -f /root/Velo/logs/frontend.log
tail -f /root/Velo/logs/ollama.log
```

## 8. 一句话结论
现在这份文档对应的是你本机当前可运行状态。服务器重启后，按 `redis -> ollama -> backend -> frontend` 的顺序启动最稳；如果后端日志出现 `✅ Redis 缓存: 连接成功`、`聊天模型预热完成`、`Rerank 模型预热完成`，就说明整套服务已经起来了。
