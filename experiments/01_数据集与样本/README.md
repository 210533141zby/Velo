# 01_数据集与样本

这一层只保留论文实验真实使用的数据，不放历史脚本或无关对比材料。

## 当前目录

- `CRUD数据源/`
  - `CRUD` 官方切分文件
  - `80k` 文档语料
- `RGB数据/`
  - 本地问答数据 `zh_refine.json`

## 与论文的对应关系

- 论文复杂多文档问答实验使用 `CRUD子样本（共1594条）`
- 其中：
  - `questanswer_2docs = 797`
  - `questanswer_3docs = 797`
- 论文稳定性支撑实验使用 `RGB 300` 数据

## 代码读取入口

- `../04_算法实现/retrieval_pipeline/datasets.py`
  - 负责读取 `CRUD` 与 `RGB` 数据

## 说明

- 本目录只保留数据源
- 具体实验脚本见 `../02_消融实验/` 与 `../03_证伪实验/`
- 结果文件中的“评测批次说明”会记录本次运行从 `CRUD子样本（1594条）` 中抽取了哪些样例
